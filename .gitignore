# Dependencies
node_modules/

# Environment variables
.env
.env.local
.env.production

# Build output
dist/

# Logs
logs/
*.log

# OS files
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Prisma
/generated/prisma

# Coverage
coverage/
.claude/

# Apex test-suite artifacts (captures, internal helpers, transient files)
scripts/apex-test-suite/baseline-*/
scripts/apex-test-suite/day1-*/
scripts/apex-test-suite/day2-*/
scripts/apex-test-suite/_*.mjs
scripts/apex-test-suite/_gate*.md
scripts/apex-test-suite/_backend-local.pid

# ── Deploy/rollback backups, file snapshots & temp artifacts (created on the
#    server during deploys — must never be committed) ──
dist.pre-*/
*.bak
*.bak-*
*.tsx.pre-*
*.ts.pre-*
*.css.pre-*
convoia_frontend_temp@*

# ── Env-file backups (hold REAL secrets — never commit) ──
.env.bak
.env.backup*
.env.*.backup*
.env.production.backup*

# ── One-off / scratch scripts (ad-hoc audits, smoke tests, prompt probes) ──
smoke-*.mjs
_oneoff_*
clawback-*.mjs
verify-mem.mjs
check-*-overcharge.mjs
scripts/model-smoke-test*
scripts/test-prompt-*.mjs

# ── Local design handoff snapshot (reference only, not part of the app build) ──
convoia-mac-handoff/
